<% if @rockhound.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      The form contains <%= pluralize(@rockhound.errors.count, "error") %>.
    </div>
    <ul>
    <% @rockhound.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

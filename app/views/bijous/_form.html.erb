<%= form_for(bijou) do |f| %>
  <% if bijou.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bijou.errors.count, "error") %> prohibited this bijou from being saved:</h2>

      <ul>
      <% bijou.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
